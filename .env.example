# Maintainer / git-clone note:
#   Use examples/.env.example → examples/.env (Gradle runs with cwd=examples/).
#   Do not copy this root pointer file to .env for local clone development.
# Release ZIPs: scripts/package.sh copies examples/.env.example to the bundle
# root as .env.example (beside examples/); ZIP recipients use that root copy.
