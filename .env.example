# Configuration for Java examples lives next to the Gradle project:
#
#   examples/.env.example   (template — copy to examples/.env to override)
#
# Run examples from the `examples/` directory so `support.Dotenv` can load those files.
